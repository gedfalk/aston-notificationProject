## Aston
Приложение для считывания event\`ов из Кафки и рассылки email`ов пользователям.

Технологии, фреймворки, паттерны:
 - Docker
 - Maven
 - Spring Boot
 - Kafka
 - GreenMail
---

### 🚀 Быстрый старт
 
`Docker-compose` со всеми нужными образами поднимается из проекта `aston-project`. В текущем же необходимо лишь запусить: 
```shell
mvn spring-boot:run
```
---

### 📡 API Endpoints
    POST /api/email/send - отправка письма

Пример запроса:
```shell 
curl -X POST http://localhost:8082/api/email/send \
-H "Content-Type: application/json" \
-d '{
"emailTo": "test@example.com",
"subject": "Тестовое письмо из API",
"message": "Текст письма, отправленного через REST API."
}'
````
---

### 🔧 Kafka
Kafka поднимается через докер из смежного проекта `aston-project` 

---

### 🧪 Тестирование
Написаны интеграционные тесты для EmailController`а и EmailService
```shell
mvn clean test
```
